package gg.neonnexus.smpplatform.lifecycle.hardcore;

/** Obsidian Gate lifecycle; vanilla Hardcore alone is deliberately not the authority. */
public enum HardcoreState {
    ALIVE,
    DEAD,
    SPECTATING,
    RESET_ELIGIBLE,
    LOCKED
}
