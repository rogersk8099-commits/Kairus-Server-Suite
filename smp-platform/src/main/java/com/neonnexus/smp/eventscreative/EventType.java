package com.neonnexus.smp.eventscreative;

/** CUSTOM deliberately preserves an extension point without weakening type validation. */
public enum EventType {
    SKY_DUELS, BLOCK_RUSH, GAUNTLET, CUSTOM
}
