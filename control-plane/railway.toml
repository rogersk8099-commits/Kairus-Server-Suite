[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "node dist/scripts/migrate.js && node dist/src/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
